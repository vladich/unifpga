# Configuration for variant 'icebreaker_no_dvi_no_tm1638_yosys'.
# Generated by tools/generate_variants.py — re-run to refresh.
# Source(s):
#   - basics-graphics-music/boards/icebreaker_no_dvi_no_tm1638_yosys/board_specific.pcf

Configuration:
  id: icebreaker_no_dvi_no_tm1638_yosys
  board: icebreaker
  toolchain: nextpnr_icestorm

  # Reset policy derived from BGM's board_specific_top.sv (tools/sync_from_bgm.py --reset)
  reset:
    sources:
      - key: 0

  # iCEBreaker P2 header: LED/BTN break-off Pmod, no user gpio (BGM no-TM1638 branch; see PLAN.md)
  attach:
    - peripheral: clock_input
      bind:
        clk: clk
    - peripheral: uart_2wire
      bind:
        tx: onboard_uart.tx
        rx: onboard_uart.rx
    - peripheral: button_array
      params:
        width: 1
      bind:
        btn: onboard_button
    - peripheral: led_bank
      params:
        width: 5
      bind:
        led: onboard_leds
    - peripheral: button_array
      params:
        width: 3
      bind:
        btn: onboard_buttons

  # External peripherals inferred from the variant directory name.
    - peripheral: inmp441_i2s_mic   # binds from BGM icebreaker_no_dvi_no_tm1638_yosys/board_specific_top.sv
      bind:
        lr: "pmod_p1a[0]"
        ws: "pmod_p1a[1]"
        sck: "pmod_p1a[2]"
        sd: "pmod_p1a[3]"
    - peripheral: i2s_audio_out   # from BGM icebreaker_no_dvi_no_tm1638_yosys/board_specific_top.sv
      bind:
        mclk: "pmod_p1b[0]"
        bclk: "pmod_p1b[1]"
        sdata: "pmod_p1b[2]"
        lrclk: "pmod_p1b[3]"
