# Configuration for variant 'tang_primer_20k_dock_no_hdmi_tm1638'.
# Generated by tools/generate_variants.py — re-run to refresh.
# Source(s):
#   - basics-graphics-music/boards/tang_primer_20k_dock_no_hdmi_tm1638/board_specific.cst

Configuration:
  id: tang_primer_20k_dock_no_hdmi_tm1638
  board: tang_primer_20k_dock
  toolchain: gowin_eda

  # Reset policy derived from BGM's board_specific_top.sv (tools/sync_from_bgm.py --reset)
  reset:
    sources:
      - key: msb

  attach:
    - peripheral: clock_input
      bind:
        clk: clk
    - peripheral: led_bank
      params:
        width: 6
      bind:
        led: onboard_leds
      lab_bits:   # bits of the lab bus this component carries (BGM's board top; tools/sync_from_bgm.py --lab-bits)
        leds: [0, 1, 2, 3, 4, 5]
    - peripheral: uart_2wire
      bind:
        tx: onboard_uart.tx
        rx: onboard_uart.rx
    - peripheral: sw_bank
      params:
        width: 4
      bind:
        sw: onboard_switches
      lab_bits:   # bits of the lab bus this component carries (BGM's board top; tools/sync_from_bgm.py --lab-bits)
        switches: [0, 1, 2, 3]
    - peripheral: button_array
      params:
        width: 5
      bind:
        btn: onboard_buttons
      lab_bits:   # bits of the lab bus this component carries (BGM's board top; tools/sync_from_bgm.py --lab-bits)
        buttons: []

  # External peripherals inferred from the variant directory name.
    - peripheral: tm1638_led_key   # binds from BGM tang_primer_20k_dock_no_hdmi_tm1638/board_specific_top.sv
      bind:
        clk: "gpio_1[2]"
        stb: "gpio_1[3]"
        dio: "gpio_1[1]"
      lab_bits:   # bits of the lab bus this component carries (BGM's board top; tools/sync_from_bgm.py --lab-bits)
        buttons: [0, 1, 2, 3, 4, 5, 6, 7]
        switches: []
        leds: [0, 1, 2, 3, 4, 5, 6, 7]
        seven_segment: [0, 1, 2, 3, 4, 5, 6, 7]
    - peripheral: inmp441_i2s_mic   # binds from BGM tang_primer_20k_dock_no_hdmi_tm1638/board_specific_top.sv
      bind:
        lr: "gpio_0[2]"
        ws: "gpio_0[3]"
        sck: "gpio_0[1]"
        sd: "gpio_0[0]"
    - peripheral: i2s_audio_out   # from BGM tang_primer_20k_dock_no_hdmi_tm1638/board_specific_top.sv
      bind:
        bclk: onboard_headphone.bck
        lrclk: onboard_headphone.ws
        sdata: onboard_headphone.din
        pa_en: onboard_pa_enable
    - peripheral: i2s_audio_out   # from BGM tang_primer_20k_dock_no_hdmi_tm1638/board_specific_top.sv
      bind:
        mclk: "gpio_1[4]"
        bclk: "gpio_1[5]"
        lrclk: "gpio_1[7]"
        sdata: "gpio_1[6]"
