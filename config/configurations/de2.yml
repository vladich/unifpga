# Configuration for variant 'de2'.
# Generated by tools/generate_variants.py — re-run to refresh.
# Source(s):
#   - basics-graphics-music/boards/de2/board_specific.qsf

Configuration:
  id: de2
  board: de2
  toolchain: quartus2

  # Reset policy derived from BGM's board_specific_top.sv (tools/sync_from_bgm.py --reset)
  reset:
    sources:
      - switch_msb: true

  attach:
    - peripheral: clock_input
      params:
        frequency_mhz: 50
      bind:
        clk: clk50mhz
    - peripheral: uart_2wire
      bind:
        tx: onboard_uart.tx
        rx: onboard_uart.rx
    - peripheral: button_array
      params:
        width: 4
      bind:
        btn: onboard_buttons
    - peripheral: led_bank
      params:
        width: 18
      bind:
        led: onboard_leds
    - peripheral: led_bank
      params:
        width: 9
      bind:
        led: onboard_leds_green
    - peripheral: sw_bank
      params:
        width: 18
      bind:
        sw: onboard_switches
    - peripheral: seven_segment_per_digit
      params:
        digits: 8
        segs: 7
      bind:
        hex: [onboard_7seg.hex0, onboard_7seg.hex1, onboard_7seg.hex2, onboard_7seg.hex3, onboard_7seg.hex4, onboard_7seg.hex5, onboard_7seg.hex6, onboard_7seg.hex7]
    - peripheral: vga_4bit   # widths: pins from the pinmap, bits from BGM w_red/w_green/w_blue
      params:
        bits_r: 10
        bits_g: 10
        bits_b: 10
        pin_bits_r: 10
        pin_bits_g: 10
        pin_bits_b: 10
        color_depth: 444
      bind:
        r: onboard_vga.r
        g: onboard_vga.g
        b: onboard_vga.b
        hs: onboard_vga.hs
        vs: onboard_vga.vs

  # External peripherals inferred from the variant directory name.
    - peripheral: inmp441_i2s_mic   # binds from BGM de2/board_specific_top.sv
      bind:
        lr: "gpio_1[0]"
        ws: "gpio_1[2]"
        sck: "gpio_1[4]"
        sd: "gpio_1[5]"
