# Configuration for variant 'tang_primer_20k_lite'.
# Generated by tools/generate_variants.py — re-run to refresh.
# Source(s):
#   - basics-graphics-music/boards/tang_primer_20k_lite/board_specific.cst

Configuration:
  id: tang_primer_20k_lite
  board: tang_primer_20k_lite
  toolchain: gowin_eda

  # Reset policy derived from BGM's board_specific_top.sv (tools/sync_from_bgm.py --reset)
  reset:
    sources:
      - key: msb

  attach:
    - peripheral: clock_input
      bind:
        clk: clk
    - peripheral: sw_bank
      params:
        width: 2
      bind:
        sw: onboard_switches
    - peripheral: button_array
      params:
        width: 2
      bind:
        btn: onboard_buttons

  # External peripherals inferred from the variant directory name.
    - peripheral: tm1638_led_key
      bind:
        clk: "gpio_2[2]"
        stb: "gpio_2[3]"
        dio: "gpio_2[1]"
    - peripheral: inmp441_i2s_mic
      bind:
        lr: "gpio_3[1]"
        ws: "gpio_3[2]"
        sck: "gpio_3[3]"
        sd: "gpio_3[0]"
