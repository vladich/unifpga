# Configuration 'icebreaker_dvi_24b_tm1638_yosys'.

Configuration:
  id: icebreaker_dvi_24b_tm1638_yosys
  board: icebreaker
  toolchain: nextpnr_icestorm

  # iCEBreaker P2 header: TM1638 + user gpio, LED/BTN break-off unused (TM1638 wiring)
  attach:
    - peripheral: clock_input
      bind:
        clk: clk
    - peripheral: uart_2wire
      bind:
        tx: onboard_uart.tx
        rx: onboard_uart.rx
    - peripheral: button_array
      params:
        width: 1
      bind:
        btn: onboard_button

  # External peripherals inferred from the variant directory name.
    - peripheral: dvi_pmod_ddr_24b
      bind:
        pmod_a: pmod_p1a
        pmod_b: pmod_p1b
    - peripheral: tm1638_led_key
      bind:
        clk: "pmod_p2[6]"
        stb: "pmod_p2[7]"
        dio: "pmod_p2[5]"
    - peripheral: gpio_header
      params:
        width: 5
      bind:
        io: ["pmod_p2[0]", "pmod_p2[1]", "pmod_p2[2]", "pmod_p2[3]", "pmod_p2[4]"]
