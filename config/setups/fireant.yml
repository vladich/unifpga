# Rig fireant: its board, what is on it and how the design sees it.
# The build expands it into the rig's configuration (./unifpga setup show fireant).

Setup:
  id: fireant
  board: fireant
  toolchain: efinity
  notes:
    - Hand-curated (Efinity XML constraints).
  use:
    - onboard: clk
    - onboard: buttons
    - onboard: leds
  extra:
    manual: true
