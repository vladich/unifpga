# Setup alinx_ax7035b_openxc7: the rig config/configurations/alinx_ax7035b_openxc7.yml describes.
# tools/setup.py generates that configuration from it (./unifpga setup check).

Setup:
  id: alinx_ax7035b_openxc7
  board: alinx_ax7035b
  toolchain: nextpnr_openxc7
  notes:
    - "alinx_ax7035b_openxc7 retargeted to nextpnr_openxc7 (open-source flow)."
    - "Mirrors alinx_ax7035b — only the toolchain differs."
  use:
    - onboard: clk
    - onboard: cpu_resetn
    - onboard: buttons
    - onboard: leds
    - onboard: 7seg
    - onboard: uart
