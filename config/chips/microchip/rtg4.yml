# Microchip (Actel) / RTG4 — chip registry
# Rad-tolerant flash FPGA (space). Libero SoC.

Family:
  id: rtg4
  producer: microchip
  name: RTG4
  description: Rad-tolerant flash FPGA (space). Libero SoC.
  default_toolchains:
    - libero_soc[*]
  chips:
    - id: RT4G150-CG1657
