# Design-wiring profile for karnix_ecp5: how the example designs use the hardware
# described in config/configurations/karnix_ecp5.yml (reset, buses, clock). Applied on
# top of it by config/init.py; synthesize.py --no-profile turns it off.
Profile:
  configuration: karnix_ecp5
  reset:
    sources:
    - key: msb
  lab_width:
    gpio: 12
  attach:
  - peripheral: sw_bank
    index: 0
    lab_bits:
      switches: []
